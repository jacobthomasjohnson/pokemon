export const createPokemon = (
      name,
      hp,
      baseSpeed,
      baseDefense,
      imgSrc,
      moveset
) => ({
      name,
      hp,
      baseSpeed,
      baseDefense,
      imgSrc,
      moveset,
});
